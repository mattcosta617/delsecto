<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/styles/style.css"/>
    <title>Questions Index</title>
</head>
<body>
    <%- include ('../partials/header.ejs'); -%>

    <main class="pagetext">
        <h1>Questions!</h1>
        <h4>Total questions asked: <%= questions.length %></h4>
        
      
        <h1> HTML</h1>
            <% for(let i = 0; i < questions.length; i++) { %>
            <div>
                <% if(questions[i].languageId === "HTML") { %>
                <a href="/questions/<%=`${questions[i]._id}`%>"> 
                   <br> <%= questions[i].question; %>
                </a> 
                <% }else if(i > questions[5]) {  %>
                      <a href="questions/html">View more</a> 
                    <%  return; %>
                  <% }}; %>
                <!-- COULD MAKE IT SO LANGUAGES ARE PART OF THE QUESTION MODEL -->
            </div>
      
        <h1>CSS</h1>
      
        <% for(let i = 0; i < questions.length; i++) { %>
            <div>
                <% if(questions[i].languageId === "CSS") { %>
                <a href="/questions/<%=`${questions[i]._id}`%>"> 
                   <br> <%= questions[i].question; %>
                </a> 
                <% }else if(i > questions[5]) {  %>
                      <a href="questions/css">View more</a> 
                    <%  return; %>
                  <% }}; %>
                <!-- COULD MAKE IT SO LANGUAGES ARE PART OF THE QUESTION MODEL -->
            </div>
            <h1>Javascript</h1>
      
            <% for(let i = 0; i < questions.length; i++) { %>
                <div>
                    <% if(questions[i].languageId === "Javascript") { %>
                    <a href="/questions/<%=`${questions[i]._id}`%>"> 
                       <br> <%= questions[i].question; %>
                    </a> 
                    <% }else if(i > questions[5]) {  %>
                          <a href="questions/Javascript">View more</a> 
                        <%  return; %>
                      <% }}; %>
                    <!-- COULD MAKE IT SO LANGUAGES ARE PART OF THE QUESTION MODEL -->
                </div>

            <h1>MongoDB</h1>
      
            <% for(let i = 0; i < questions.length; i++) { %>
                <div>
                    <% if(questions[i].languageId === "MongoDB") { %>
                    <a href="/questions/<%=`${questions[i]._id}`%>"> 
                       <br> <%= questions[i].question; %>
                    </a> 
                    <% }else if(i > questions[5]) {  %>
                          <a href="questions/MongoDB">View more</a> 
                        <%  return; %>
                      <% }}; %>
                    <!-- COULD MAKE IT SO LANGUAGES ARE PART OF THE QUESTION MODEL -->
                </div>

            <h1>Python</h1>
      
            <% for(let i = 0; i < questions.length; i++) { %>
                <div>
                    <% if(questions[i].languageId === "Python") { %>
                    <a href="/questions/<%=`${questions[i]._id}`%>"> 
                       <br> <%= questions[i].question; %>
                    </a> 
                    <% }else if(i > questions[5]) {  %>
                          <a href="questions/Python">View more</a> 
                        <%  return; %>
                      <% }}; %>
                    <!-- COULD MAKE IT SO LANGUAGES ARE PART OF THE QUESTION MODEL -->
                </div>

            <h1>Express</h1>
      
            <% for(let i = 0; i < questions.length; i++) { %>
                <div>
                    <% if(questions[i].languageId === "Express") { %>
                    <a href="/questions/<%=`${questions[i]._id}`%>"> 
                       <br> <%= questions[i].question; %>
                    </a> 
                    <% }else if(i > questions[5]) {  %>
                          <a href="questions/Express">View more</a> 
                        <%  return; %>
                      <% }}; %>
                    <!-- COULD MAKE IT SO LANGUAGES ARE PART OF THE QUESTION MODEL -->
                </div>
    
    </main>
</body>
</html>